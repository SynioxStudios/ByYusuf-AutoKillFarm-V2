local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RS = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")

_G.KillActive = true
_G.HopInterval = 60
local SecretAreaPos = CFrame.new(1947, 2, 6191)
local playerWhitelist = {}

task.spawn(function()
    local function setupCharacter(char)
        local hrp = char:WaitForChild("HumanoidRootPart")
        hrp.CFrame = SecretAreaPos
        local bodyVelocity = Instance.new("BodyVelocity")
        bodyVelocity.MaxForce = Vector3.new(100000, 0, 100000)
        bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        bodyVelocity.P = 1250
        bodyVelocity.Parent = hrp
    end
    if LocalPlayer.Character then setupCharacter(LocalPlayer.Character) end
    LocalPlayer.CharacterAdded:Connect(setupCharacter)
end)

task.spawn(function()
    while _G.KillActive do
        pcall(function()
            local char = LocalPlayer.Character
            local punch = LocalPlayer.Backpack:FindFirstChild("Punch") or (char and char:FindFirstChild("Punch"))
            if punch then
                if punch.Parent ~= char then punch.Parent = char end
                
                LocalPlayer.muscleEvent:FireServer("punch", "rightHand")
                LocalPlayer.muscleEvent:FireServer("punch", "leftHand")
                
                local rHand = char:FindFirstChild("RightHand")
                if rHand then
                    for _, target in ipairs(Players:GetPlayers()) do
                        if target ~= LocalPlayer and not playerWhitelist[target.Name] and target.Character then
                            local tRoot = target.Character:FindFirstChild("HumanoidRootPart")
                            local tHum = target.Character:FindFirstChild("Humanoid")
                            if tRoot and tHum and tHum.Health > 0 then
                                tRoot.Size = Vector3.new(15, 15, 15)
                                tRoot.CanCollide = false
                                firetouchinterest(rHand, tRoot, 1)
                                firetouchinterest(rHand, tRoot, 0)
                            end
                        end
                    end
                end
            end
        end)
        task.wait(0.01)
    end
end)

local function forceHop()
    local success, result = pcall(function()
        return HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Desc&limit=100")).data
    end)
    if success and result then
        for _, server in pairs(result) do
            if server.playing >= 18 and server.playing < server.maxPlayers and server.id ~= game.JobId then
                TeleportService:TeleportToPlaceInstance(game.PlaceId, server.id, LocalPlayer)
                task.wait(1.5)
            end
        end
    end
    task.wait(1); forceHop()
end

task.spawn(function()
    local t = _G.HopInterval
    while t > 0 do
        _G.CurrentHop = t
        task.wait(1); t = t - 1
    end
    forceHop()
end)

local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
local MainBack = Instance.new("Frame", ScreenGui)
MainBack.Size = UDim2.new(0, 180, 0, 70)
MainBack.Position = UDim2.new(0, 15, 0.5, -35)
MainBack.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
MainBack.BackgroundTransparency = 0.4
MainBack.BorderSizePixel = 0
Instance.new("UICorner", MainBack).CornerRadius = UDim.new(0, 8)

local Info = Instance.new("TextLabel", MainBack)
Info.Size = UDim2.new(1, -20, 1, -10)
Info.Position = UDim2.new(0, 10, 0, 5)
Info.BackgroundTransparency = 1
Info.TextColor3 = Color3.fromRGB(200, 200, 200)
Info.Font = Enum.Font.Code
Info.TextSize = 13
Info.TextXAlignment = Enum.TextXAlignment.Left

task.spawn(function()
    while task.wait(0.05) do
        local kills = LocalPlayer:FindFirstChild("leaderstats") and LocalPlayer.leaderstats:FindFirstChild("Kills")
        Info.Text = string.format(
            "SYNİOX KİLL PAİD\n\nKills: %s\nHop: %ds", 
            (kills and tostring(kills.Value) or "0"), 
            _G.CurrentHop or 0
        )
    end
end)
